<div class="form-group">
    
    <label>الموكل</label>
    <select name="customer" class="form-control selectpicker" data-live-search="true" >
      <% customers.forEach(customer => { %>
        <% if (customer.id === authoriz.customer) { %>
          <option selected label="<%= customer.name %>" value="<%= customer.id %>"><%= customer.name %></option>
        <% } else { %>
          <option label="<%= customer.name %>" value="<%= customer.id %>"><%= customer.name %></option>
        <% } %>
      <% }) %>
   
      </select>
 
</div>

<div class="form-group" >
  <input type="text" class="form-control"  name="authorizdate" placeholder="تاريخ التوكيل" required="">
</div>

<div class="form-group" style="width: 200px;">
    <input  type="file" class="filepond"  name="cover" placeholder="صورة الوكالة" required="">
</div>


